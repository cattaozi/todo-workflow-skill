
---

## 工作流（文件驱动的产品/工程管理）

> **启动协议**：下方通过 `@` 导入的 bootstrap 内容是本 session 的运行时合约。
> 进入项目时按 bootstrap 协议扫一眼当前 TODO / 预研状态，再回应用户。
> 具体任务优先于汇报。

@.workflow/skill/bootstrap/SKILL.md

> 其它 Skill（todo-create / todo-progress / prd-review / explore）按需 Read，**不**用 `@` 导入。
> 工作流细节见 [`.workflow/README.md`](.workflow/README.md)。

## Git 工作流

**用户没说"提交"之前，绝不自动 `git commit`。**

完整 commit 规范见 [`.workflow/README.md`](.workflow/README.md)，要点：

- `<type>(<scope>): <一句话，含 TODO 编号>`
- body 必含 TODO 编号清单 + 关键决策一段
- 用具体路径 `git add`，不用 `-A`（防 `.env` 误入）
- 方括号路径（如 `[id]` 路由）必须用引号
- 永远不 force push 到 main
